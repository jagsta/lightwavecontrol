#include <Arduino.h>
#include <Ethernet.h>
extern const char clientId[], username[], password[], subtopic[], pubtopic[], snooptopic[];
//extern IPAddress gateway, netmask, ip, server;
extern byte id[], mac[], gateway[], netmask[], ip[], server[];;
