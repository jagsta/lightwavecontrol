#include <Arduino.h>
#include <Ethernet.h>
extern const char clientId[], username[], password[], subtopic[], pubtopici[], snooptopic[];
extern IPAddress gateway, netmask, ip, server;
extern byte id[], mac[];
